
int	main()
{
	// try open file
	// tokenize (from a file instead of a string?)
	// ParseConfig tree
	// ConfigValidator tree
	// Config config = Config::build(tree);

	// ServerManager createServers(Config)
}